if (require("devtools")){
   pkg <- as.package("pkg")
   cat("use 'load_all()' to load tabplot sources\n")
}
if (require("knitr")) {
	vig <- function() {
		knit2pdf("./pkg/inst/doc/tabplot-vignette.Rnw", "./pkg/inst/doc/tabplot-vignette.tex")
	}
	cat("use 'vig()' to create vignette\n")
}
