#' tableplot object that contains the information to plot a tableplot.
#'
#' A \code{tabplot}-object is an object of class \code{tabplot} that contains the information to plot a tableplot without the steps that may be time-consuming, such as sorting and aggregating.
#' The function \code{\link{tabplot}} returns a \code{tabplot}-object. Use \code{plot=FALSE} to 
#' supress that the tableplot is plot. The function \code{\link{changeTabplot}} can be used to change a \code{tabplot}-object.
#'
#' @name tabplot-object
#' @aliases tabplot-object
{}