#' tableplot object that contains the information to plot a tableplot.
#'
#' A tabplot-object is an object of class \code{tabplot} that contains the information to plot a tableplot without the steps that may be time-consuming, such as sorting and aggregating.
#' The function \code{\link{tableplot}} returns a tabplot-object. Use \code{plot=FALSE} to 
#' supress that the tableplot is plot. The function \code{\link{changeTabplot}} can be used to change a tabplot-object. The generic functions \code{\link[=plot.tabplot]{plot}} and \code{\link[=summary.tabplot]{summary}} are used to plot and summarize a tabplot-object.
#'
#' @name tabplot-object
{}